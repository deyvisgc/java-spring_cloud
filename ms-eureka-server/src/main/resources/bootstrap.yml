spring:
  application:
    name: registry-service
  cloud:
    config:
      import-check:
        enabled: false
      uri: http://localhost:8081
      label: master
      username: root
      password: s3cr3t
logging:
  level:
    org.springframework.cloud.config: DEBUG